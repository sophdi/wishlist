<!-- views/auth/login.ejs-->
<!DOCTYPE html>
<html lang="uk">

<head>
  <%- include('../partials/head') %>
</head>

<body class="bg-brand-bg min-h-screen flex items-center justify-center p-4">
  <%- include('../partials/navbar-guest') %>
  <div class="w-full max-w-md">
    <div class="bg-white rounded-3xl shadow-2xl overflow-hidden">
      <div class="p-10">
        <div class="text-center mb-10">
          <div class="w-20 h-20 bg-pink-100 rounded-2xl flex items-center justify-center mx-auto mb-6">
            <span class="text-4xl">🎁</span>
          </div>
          <h1 class="text-4xl font-bold text-brand-text mb-3">Вхід</h1>
          <p class="text-gray-600">Продовж втілювати свої мрії</p>
        </div>

        <% if (errorMessage) { %>
        <div class="text-red-500 text-sm mb-4"><%= errorMessage %></div>
        <% } %>

        <form action="/auth/login" method="POST" class="space-y-6">
          <div>
            <label for="email" class="sr-only">Email</label>
            <div class="relative">
              <input type="email" id="email" name="email"
                class="w-full px-4 py-3 pl-12 border-2 border-gray-200 rounded-xl focus:outline-none focus:border-brand-primary transition duration-300"
                placeholder="Email" required autocomplete="username" pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$"
                title="Введіть коректний email" />
              <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                <i class="fas fa-envelope text-gray-400"></i>
              </div>
            </div>
          </div>

          <div>
            <label for="password" class="sr-only">Пароль</label>
            <div class="relative">
              <input type="password" id="password" name="password"
                class="w-full px-4 py-3 pl-12 border-2 border-gray-200 rounded-xl focus:outline-none focus:border-brand-primary transition duration-300"
                placeholder="Пароль" required autocomplete="current-password" />
              <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                <i class="fas fa-lock text-gray-400"></i>
              </div>
            </div>
          </div>
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <button type="submit"
            class="w-full py-3 bg-brand-text text-white rounded-xl hover:bg-gray-800 transition duration-300">
            Увійти
          </button>
        </form>

        <div class="text-center mt-8">
          <p class="text-gray-600 mb-4">
            Ще не маєте акаунта?
            <a href="/auth/register" class="text-brand-primary font-medium hover:underline">
              Зареєструватися
            </a>
          </p>
        </div>
      </div>
    </div>
  </div>
  <script src="/js/login.js" defer></script>
</body>

</html>