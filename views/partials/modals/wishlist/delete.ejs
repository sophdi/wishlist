<!-- Delete Confirmation Modal -->
<div id="deleteWishlistConfirmModal" class="fixed inset-0 hidden z-50 flex items-center justify-center p-4">
  <div class="absolute inset-0" style="background-color: rgba(0,0,0,0.4);"></div>
  <div class="bg-white rounded-2xl w-full max-w-md text-center p-8 relative z-10">
    <div class="mb-4">
      <i class="fas fa-trash-alt text-3xl text-red-500"></i>
    </div>
    <h3 class="text-2xl font-semibold text-gray-900 mb-4">
      Видалити список бажань?
    </h3>
    <p class="text-gray-600 mb-6">
      Усі бажання в цьому списку будуть видалені. Цю дію неможливо буде скасувати.
    </p>
    <form action="/wishlists/<%= wishlist.id %>/delete" method="POST" class="flex gap-4">
      <button type="button" 
              onclick="closeDeleteConfirmModal()" 
              class="flex-1 bg-gray-200 text-gray-700 py-3 rounded-lg">
        Скасувати
      </button>
      <button type="submit" 
              class="flex-1 bg-red-500 text-white py-3 rounded-lg">
        Видалити
      </button>
    </form>
  </div>
</div>