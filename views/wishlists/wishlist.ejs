<!DOCTYPE html>
<html lang="uk">
<head>
  <%- include('../partials/head') %>
</head>
<body class="bg-white min-h-screen flex flex-col">
  <%- include('../partials/navbar') %>
  <main class="container mx-auto px-4 py-12 pt-24 flex-grow">
    <%- include('../partials/alerts') %>
    <div class="max-w-7xl mx-auto">
      <section class="mb-12">
        <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
          <div>
            <h1 class="text-4xl font-bold text-gray-900 mb-2">Мої вішлісти</h1>
            <p class="text-gray-600">Керуй своїми мріями</p>
          </div>
          <div class="flex items-center gap-4">
            <div class="inline-flex shadow-sm rounded-md">
              <button type="button" 
                      class="view-toggle py-2 px-3 text-sm font-medium rounded-l-md border border-gray-300 bg-white text-gray-700 hover:bg-gray-50 focus:z-10 focus:ring-2 focus:ring-primary-500 focus:border-primary-500 active" 
                      data-view="grid">
                <i class="fas fa-th"></i>
              </button>
              <button type="button" 
                      class="view-toggle py-2 px-3 text-sm font-medium rounded-r-md border border-gray-300 bg-white text-gray-700 hover:bg-gray-50 focus:z-10 focus:ring-2 focus:ring-primary-500 focus:border-primary-500" 
                      data-view="list">
                <i class="fas fa-list"></i>
              </button>
            </div>
            <button onclick="openCreateModal()" 
                    class="bg-gray-900 text-white px-6 py-3 rounded-lg flex items-center gap-2">
              <i class="fas fa-plus"></i>
              <span>Створити вішліст</span>
            </button>
          </div>
        </div>
      </section>
      <section>
        <% if (wishlists.length > 0) { %>
          <div id="wishlistsContainer" class="grid-mode">
            <!-- Add Create Wishlist Card First -->
            <div class="wishlist-item grid-view">
              <div class="bg-white rounded-lg border-2 border-dashed border-gray-300 flex items-center justify-center p-6 cursor-pointer hover:border-primary-500 hover:bg-primary-50 transition-all duration-300 h-full">
                <div class="text-gray-400 mr-3">
                  <i class="fas fa-plus-circle text-2xl"></i>
                </div>
                <p class="text-sm font-medium text-gray-500">Створити новий вішлист</p>
              </div>
            </div>

            <!-- Existing Wishlists -->
            <% wishlists.forEach(wishlist => { %>
              <div class="wishlist-item grid-view">
                <a href="/wishlists/<%= wishlist.id %>" class="block">
                  <div class="bg-white border border-gray-200 rounded-2xl p-6 transition-all duration-300">
                    <div class="flex justify-between items-start mb-4">
                      <h3 class="text-xl font-semibold text-gray-900 truncate pr-4"><%= wishlist.title %></h3>
                      <div class="w-10 h-10 bg-gray-100 rounded-xl flex items-center justify-center flex-shrink-0">
                        <i class="fas fa-gift text-gray-900"></i>
                      </div>
                    </div>
                    <div class="mb-4 min-h-[3rem]">
                      <% if (wishlist.description) { %>
                        <p class="text-gray-600 line-clamp-2"><%= wishlist.description %></p>
                      <% } else { %>
                        <p class="text-gray-400 italic">Без опису</p>
                      <% } %>
                    </div>
                    <div class="text-sm text-gray-500 space-y-2">
                      <div class="flex items-center gap-2">
                        <i class="fas fa-list"></i>
                        <span><%= wishlist.items_count || 0 %> бажань</span>
                      </div>
                      <div class="flex items-center gap-2">
                        <i class="fas fa-calendar"></i>
                        <span>Створено <%= new Date(wishlist.created_at).toLocaleDateString('uk-UA') %></span>
                      </div>
                    </div>
                  </div>
                </a>
              </div>
            <% }) %>
          </div>
        <% } else { %>
          <div class="text-center bg-gray-50 rounded-2xl p-12">
            <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-6">
              <i class="fas fa-gift text-4xl text-gray-900"></i>
            </div>
            <h2 class="text-2xl font-semibold text-gray-900 mb-3">Ще немає вішлістів</h2>
            <p class="text-gray-600 mb-6 max-w-md mx-auto">Розпочни свою подорож мрій. Створи перший вішліст прямо зараз!</p>
            <button onclick="openCreateModal()" class="bg-gray-900 text-white px-6 py-3 rounded-lg">Створити вішліст</button>
          </div>
        <% } %>
      </section>
    </div>
  </main>  
<%- include('../partials/wishlist-modals', { wishlist: { id: 0, title: '', description: '' } }) %>  
  <%- include('../partials/footer') %>
  <script src="/js/wishlist-modals.js"></script>
  <script src="/js/wishlist-view.js"></script>
</body>
</html>