<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Мої списки бажань</title>
</head>
<body>
    <h1>Мої списки бажань</h1>
    <form action="/wishlists/create" method="POST">
        <input type="text" name="title" placeholder="Назва списку" required>
        <button type="submit">Додати</button>
    </form>

    <ul>
        <% wishlists.forEach(wishlist => { %>
            <li>
                <%= wishlist.title %>
                <a href="/wishlists/delete/<%= wishlist.id %>">Видалити</a>
            </li>
        <% }) %>
    </ul>

    <a href="/">На головну</a>
</body>
</html>